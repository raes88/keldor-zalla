class Tirada extends Partida {
    constructor(data) {
        super(data.tirada, data.bolos)
    }
    actualizar() {

        // this.tirada++;
        // const tiradaExtra = 0
        // if (this.tirada < 3 && tiradaExtra == 0) {
        //     this.bolos = Math.round(Math.random() * 10);
        //     if (this.bolos < 10) {
        //         const total = total + this.bolos
        //         tiradaExtra++;

        //     } else {
        //         const total = total + this.bolos
        //         tiradaExtra++;
        //     }
        // }else{
        // 	 this.bolos = Math.round(Math.random() * 10);
        // }
    }

}